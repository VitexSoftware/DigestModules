#!/bin/bash
set -e

case "$1" in
    configure)
        # Autoloader is now shipped in the .deb at:
        # /usr/share/php/VitexSoftware/DigestModules/autoload.php
        # No runtime composer operations needed
        ;;
esac

#DEBHELPER#

exit 0